<Window x:Class="ASI_GuessTheNumber.View.StartGamePopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ASI_GuessTheNumber.View"
        WindowStyle="None"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        AllowsTransparency="True">

    <Border Background="White"
            CornerRadius="10"
            BorderBrush="#CCCCCC"
            BorderThickness="1"
            Padding="20"
            Width="300">

        <StackPanel>

            <TextBlock Text="Start New Game"
                       FontSize="18"
                       FontWeight="Bold"
                       Foreground="#2A6F97"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>

            <TextBlock Text="Choose a range:"
                       FontSize="14"
                       Margin="0,0,0,5"/>

            <ComboBox ItemsSource="{Binding RangeOptions}"
                      SelectedItem="{Binding SelectedRange}"
                      FontSize="14"
                      Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        >

                <Button Content="Start"
                        Width="90"
                        Padding="5"
                        Command="{Binding StartCommand}"
                        Background="#2A6F97"
                        Foreground="White"
                        BorderThickness="0"/>

                <Button Content="Cancel"
                        Width="90"
                        Padding="5"
                        Command="{Binding CancelCommand}"
                        Background="#DDDDDD"
                        Foreground="Black"
                        BorderThickness="0"/>
            </StackPanel>

        </StackPanel>
    </Border>
</Window>

